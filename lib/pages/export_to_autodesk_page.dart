import 'package:flutter/material.dart';

class ExportToAutodeskPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Экспорт в Autodesk'),
      ),
      body: Center(
        child: Text(
          'Страница экспорта в Autodesk находится в разработке.',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
